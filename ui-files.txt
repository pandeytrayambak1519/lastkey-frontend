@import "tailwindcss";

:root {
  font-family:
    Inter, ui-sans-serif, system-ui, -apple-system,
    BlinkMacSystemFont, "Segoe UI", sans-serif;

  color: #0f172a;
  background-color: #f8fafc;

  font-synthesis: none;
  text-rendering: optimizeLegibility;

  --lastkey-primary: #2563eb;
  --lastkey-primary-dark: #1d4ed8;
  --lastkey-navy: #0f172a;
  --lastkey-deep-navy: #020617;
  --lastkey-background: #f8fafc;
  --lastkey-card: #ffffff;
  --lastkey-border: #e2e8f0;
  --lastkey-muted: #64748b;
  --lastkey-success: #16a34a;
  --lastkey-warning: #d97706;
  --lastkey-danger: #dc2626;
}

* {
  box-sizing: border-box;
}

html {
  min-width: 320px;
  min-height: 100%;
  scroll-behavior: smooth;
}

body {
  min-width: 320px;
  min-height: 100vh;
  margin: 0;

  background:
    radial-gradient(
      circle at top right,
      rgba(37, 99, 235, 0.05),
      transparent 28rem
    ),
    #f8fafc;

  color: #0f172a;
}

body,
button,
input,
textarea,
select {
  font-family: inherit;
}

button,
a,
input,
textarea,
select {
  -webkit-tap-highlight-color: transparent;
}

button {
  cursor: pointer;
}

button:disabled {
  cursor: not-allowed;
}

img,
svg {
  display: block;
}

img {
  max-width: 100%;
}

a {
  color: inherit;
  text-decoration: none;
}

::selection {
  background-color: rgba(37, 99, 235, 0.18);
  color: #0f172a;
}

::-webkit-scrollbar {
  width: 9px;
  height: 9px;
}

::-webkit-scrollbar-track {
  background: #f1f5f9;
}

::-webkit-scrollbar-thumb {
  border: 2px solid #f1f5f9;
  border-radius: 999px;
  background: #94a3b8;
}

::-webkit-scrollbar-thumb:hover {
  background: #64748b;
}

.auth-background {
  background:
    radial-gradient(
      circle at 15% 15%,
      rgba(59, 130, 246, 0.24),
      transparent 28%
    ),
    radial-gradient(
      circle at 85% 80%,
      rgba(124, 58, 237, 0.16),
      transparent 25%
    ),
    linear-gradient(
      145deg,
      #020617 0%,
      #0f172a 50%,
      #111827 100%
    );
}

.glass-panel {
  border: 1px solid rgba(255, 255, 255, 0.12);
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(18px);
}

.focus-ring {
  outline: none;
  transition:
    border-color 160ms ease,
    box-shadow 160ms ease;
}

.focus-ring:focus {
  border-color: #3b82f6;
  box-shadow:
    0 0 0 4px rgba(59, 130, 246, 0.12);
}

.page-enter {
  animation: page-enter 350ms ease both;
}

@keyframes page-enter {
  from {
    opacity: 0;
    transform: translateY(6px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slide-in {
  from {
    opacity: 0;
    transform: translateX(-100%);
  }

  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.shimmer {
  position: relative;
  overflow: hidden;
  background-color: #e2e8f0;
}

.shimmer::after {
  position: absolute;
  inset: 0;

  content: "";

  transform: translateX(-100%);

  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.65),
    transparent
  );

  animation: shimmer 1.5s infinite;
}

@keyframes shimmer {
  100% {
    transform: translateX(100%);
  }
}

.landing-hero {
  background:
    radial-gradient(
      circle at 15% 20%,
      rgba(59, 130, 246, 0.1),
      transparent 28rem
    ),
    radial-gradient(
      circle at 90% 80%,
      rgba(124, 58, 237, 0.08),
      transparent 26rem
    ),
    linear-gradient(
      180deg,
      #ffffff 0%,
      #f8fafc 100%
    );
}

.landing-grid {
  background-image:
    linear-gradient(
      rgba(15, 23, 42, 0.04) 1px,
      transparent 1px
    ),
    linear-gradient(
      90deg,
      rgba(15, 23, 42, 0.04) 1px,
      transparent 1px
    );

  background-size: 42px 42px;

  mask-image: linear-gradient(
    to bottom,
    black,
    transparent 85%
  );
}

.landing-grid-dark {
  background-image:
    linear-gradient(
      rgba(255, 255, 255, 0.04) 1px,
      transparent 1px
    ),
    linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.04) 1px,
      transparent 1px
    );

  background-size: 40px 40px;
}

.landing-page-header {
  background:
    radial-gradient(
      circle at top center,
      rgba(59, 130, 246, 0.1),
      transparent 30rem
    ),
    linear-gradient(
      180deg,
      #ffffff 0%,
      #f8fafc 100%
    );

  border-bottom: 1px solid #e2e8f0;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }

  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}.counter {
  font-size: 16px;
  padding: 5px 10px;
  border-radius: 5px;
  color: var(--accent);
  background: var(--accent-bg);
  border: 2px solid transparent;
  transition: border-color 0.3s;
  margin-bottom: 24px;

  &:hover {
    border-color: var(--accent-border);
  }
  &:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }
}

.hero {
  position: relative;

  .base,
  .framework,
  .vite {
    inset-inline: 0;
    margin: 0 auto;
  }

  .base {
    width: 170px;
    position: relative;
    z-index: 0;
  }

  .framework,
  .vite {
    position: absolute;
  }

  .framework {
    z-index: 1;
    top: 34px;
    height: 28px;
    transform: perspective(2000px) rotateZ(300deg) rotateX(44deg) rotateY(39deg)
      scale(1.4);
  }

  .vite {
    z-index: 0;
    top: 107px;
    height: 26px;
    width: auto;
    transform: perspective(2000px) rotateZ(300deg) rotateX(40deg) rotateY(39deg)
      scale(0.8);
  }
}

#center {
  display: flex;
  flex-direction: column;
  gap: 25px;
  place-content: center;
  place-items: center;
  flex-grow: 1;

  @media (max-width: 1024px) {
    padding: 32px 20px 24px;
    gap: 18px;
  }
}

#next-steps {
  display: flex;
  border-top: 1px solid var(--border);
  text-align: left;

  & > div {
    flex: 1 1 0;
    padding: 32px;
    @media (max-width: 1024px) {
      padding: 24px 20px;
    }
  }

  .icon {
    margin-bottom: 16px;
    width: 22px;
    height: 22px;
  }

  @media (max-width: 1024px) {
    flex-direction: column;
    text-align: center;
  }
}

#docs {
  border-right: 1px solid var(--border);

  @media (max-width: 1024px) {
    border-right: none;
    border-bottom: 1px solid var(--border);
  }
}

#next-steps ul {
  list-style: none;
  padding: 0;
  display: flex;
  gap: 8px;
  margin: 32px 0 0;

  .logo {
    height: 18px;
  }

  a {
    color: var(--text-h);
    font-size: 16px;
    border-radius: 6px;
    background: var(--social-bg);
    display: flex;
    padding: 6px 12px;
    align-items: center;
    gap: 8px;
    text-decoration: none;
    transition: box-shadow 0.3s;

    &:hover {
      box-shadow: var(--shadow);
    }
    .button-icon {
      height: 18px;
      width: 18px;
    }
  }

  @media (max-width: 1024px) {
    margin-top: 20px;
    flex-wrap: wrap;
    justify-content: center;

    li {
      flex: 1 1 calc(50% - 8px);
    }

    a {
      width: 100%;
      justify-content: center;
      box-sizing: border-box;
    }
  }
}

#spacer {
  height: 88px;
  border-top: 1px solid var(--border);
  @media (max-width: 1024px) {
    height: 48px;
  }
}

.ticks {
  position: relative;
  width: 100%;

  &::before,
  &::after {
    content: '';
    position: absolute;
    top: -4.5px;
    border: 5px solid transparent;
  }

  &::before {
    left: 0;
    border-left-color: var(--border);
  }
  &::after {
    right: 0;
    border-right-color: var(--border);
  }
}
import { LoaderCircle } from "lucide-react";

const variants = {
  primary:
    "bg-blue-600 text-white shadow-lg shadow-blue-600/20 hover:bg-blue-700 focus:ring-blue-500/20",

  dark:
    "bg-slate-950 text-white shadow-lg shadow-slate-950/20 hover:bg-slate-800 focus:ring-slate-500/20",

  secondary:
    "border border-slate-200 bg-white text-slate-700 shadow-sm hover:border-slate-300 hover:bg-slate-50 focus:ring-slate-500/10",

  danger:
    "bg-red-600 text-white shadow-lg shadow-red-600/20 hover:bg-red-700 focus:ring-red-500/20",

  ghost:
    "bg-transparent text-slate-600 hover:bg-slate-100 hover:text-slate-950 focus:ring-slate-500/10",
};

const sizes = {
  small: "h-9 rounded-xl px-4 text-sm",

  medium: "h-11 rounded-xl px-5 text-sm",

  large: "h-12 rounded-2xl px-6 text-sm",
};

export default function Button({
  children,
  type = "button",
  variant = "primary",
  size = "large",
  loading = false,
  loadingText = "Please wait...",
  fullWidth = false,
  leftIcon: LeftIcon,
  rightIcon: RightIcon,
  disabled = false,
  className = "",
  ...props
}) {
  const isDisabled = disabled || loading;

  return (
    <button
      type={type}
      disabled={isDisabled}
      className={[
        "inline-flex items-center justify-center gap-2 font-semibold",
        "transition-all duration-200",
        "focus:outline-none focus:ring-4",
        "disabled:cursor-not-allowed disabled:opacity-60",
        "active:scale-[0.98]",
        variants[variant] || variants.primary,
        sizes[size] || sizes.large,
        fullWidth ? "w-full" : "",
        className,
      ].join(" ")}
      {...props}
    >
      {loading ? (
        <>
          <LoaderCircle className="h-4 w-4 animate-spin" />
          <span>{loadingText}</span>
        </>
      ) : (
        <>
          {LeftIcon && (
            <LeftIcon className="h-4 w-4 shrink-0" />
          )}

          <span>{children}</span>

          {RightIcon && (
            <RightIcon className="h-4 w-4 shrink-0" />
          )}
        </>
      )}
    </button>
  );
}import React from 'react'

const Card = () => <div>Card</div>

export default Card
import {
  ArrowRight,
  CheckCircle2,
  FileCheck2,
  KeyRound,
  LockKeyhole,
  ShieldCheck,
  Sparkles,
  UsersRound,
} from "lucide-react";
import { Link } from "react-router-dom";

import Button from "../ui/Button";
import { useAuth } from "../../hooks/useAuth";
import { ROUTES } from "../../utils/routePaths";

const heroBenefits = [
  "Encrypted document storage",
  "Trusted nominee workflows",
  "Verified emergency release",
];

export default function HeroSection() {
  const {
    isAuthenticated,
  } = useAuth();

  return (
    <section className="landing-hero relative overflow-hidden">
      <div className="landing-grid absolute inset-0 opacity-40" />

      <div className="absolute -left-32 top-10 h-96 w-96 rounded-full bg-blue-500/15 blur-3xl" />

      <div className="absolute -right-40 bottom-0 h-[420px] w-[420px] rounded-full bg-violet-500/10 blur-3xl" />

      <div className="relative mx-auto grid max-w-[1440px] items-center gap-14 px-4 py-18 sm:px-6 sm:py-24 lg:grid-cols-[1fr_0.95fr] lg:px-8 lg:py-28">
        <div>
          <div className="inline-flex items-center gap-2 rounded-full border border-blue-200 bg-blue-50 px-4 py-2 text-xs font-semibold text-blue-700">
            <Sparkles className="h-4 w-4" />
            Built for modern families and trusted advisors
          </div>

          <h1 className="mt-7 text-4xl font-black tracking-tight text-slate-950 sm:text-5xl lg:text-6xl xl:text-7xl">
            Protect your digital life.
            <span className="block bg-gradient-to-r from-blue-600 to-violet-600 bg-clip-text text-transparent">
              Empower the people you trust.
            </span>
          </h1>

          <p className="mt-6 max-w-2xl text-base leading-8 text-slate-600 sm:text-lg">
            LastKey gives you a secure vault for documents, nominee permissions for trusted people, and a verified release workflow for emergencies.
          </p>

          <div className="mt-8 flex flex-col gap-3 sm:flex-row sm:items-center">
            <Link
              to={
                isAuthenticated
                  ? ROUTES.DASHBOARD
                  : ROUTES.REGISTER
              }
            >
              <Button
                fullWidth
                className="sm:w-auto"
                rightIcon={ArrowRight}
              >
                {isAuthenticated
                  ? "Open dashboard"
                  : "Create secure vault"}
              </Button>
            </Link>

            <Link to={ROUTES.FEATURES}>
              <Button
                fullWidth
                className="sm:w-auto"
                variant="secondary"
              >
                Explore features
              </Button>
            </Link>
          </div>

          <div className="mt-8 flex flex-wrap gap-x-6 gap-y-3">
            {heroBenefits.map(
              (benefit) => (
                <div
                  key={benefit}
                  className="flex items-center gap-2 text-sm font-semibold text-slate-600"
                >
                  <CheckCircle2 className="h-4 w-4 text-emerald-600" />

                  {benefit}
                </div>
              ),
            )}
          </div>

          <div className="mt-10 flex flex-wrap items-center gap-6 border-t border-slate-200 pt-7">
            <div>
              <p className="text-2xl font-black text-slate-950">
                256-bit
              </p>

              <p className="mt-1 text-xs font-semibold text-slate-500">
                Encryption ready
              </p>
            </div>

            <div className="h-10 w-px bg-slate-200" />

            <div>
              <p className="text-2xl font-black text-slate-950">
                24/7
              </p>

              <p className="mt-1 text-xs font-semibold text-slate-500">
                Secure availability
              </p>
            </div>

            <div className="h-10 w-px bg-slate-200" />

            <div>
              <p className="text-2xl font-black text-slate-950">
                100%
              </p>

              <p className="mt-1 text-xs font-semibold text-slate-500">
                Controlled access
              </p>
            </div>
          </div>
        </div>

        <div className="relative mx-auto w-full max-w-2xl">
          <div className="absolute -inset-5 rounded-[38px] bg-gradient-to-br from-blue-500/20 to-violet-500/20 blur-2xl" />

          <div className="relative overflow-hidden rounded-[32px] border border-slate-200 bg-white p-3 shadow-2xl shadow-slate-950/15">
            <div className="overflow-hidden rounded-[24px] border border-slate-200 bg-slate-50">
              <div className="flex items-center justify-between border-b border-slate-200 bg-white px-5 py-4">
                <div className="flex items-center gap-3">
                  <span className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-950 text-white">
                    <KeyRound className="h-5 w-5" />
                  </span>

                  <div>
                    <p className="text-sm font-extrabold text-slate-950">
                      LastKey Vault
                    </p>

                    <p className="text-[11px] text-slate-500">
                      Secure overview
                    </p>
                  </div>
                </div>

                <span className="inline-flex items-center gap-1.5 rounded-full bg-emerald-50 px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-emerald-700">
                  <ShieldCheck className="h-3.5 w-3.5" />
                  Protected
                </span>
              </div>

              <div className="grid gap-4 p-5 sm:grid-cols-3">
                <div className="rounded-2xl border border-blue-100 bg-blue-50 p-4">
                  <FileCheck2 className="h-5 w-5 text-blue-600" />

                  <p className="mt-4 text-2xl font-black text-slate-950">
                    24
                  </p>

                  <p className="mt-1 text-xs font-semibold text-slate-500">
                    Secure files
                  </p>
                </div>

                <div className="rounded-2xl border border-violet-100 bg-violet-50 p-4">
                  <UsersRound className="h-5 w-5 text-violet-600" />

                  <p className="mt-4 text-2xl font-black text-slate-950">
                    3
                  </p>

                  <p className="mt-1 text-xs font-semibold text-slate-500">
                    Nominees
                  </p>
                </div>

                <div className="rounded-2xl border border-emerald-100 bg-emerald-50 p-4">
                  <LockKeyhole className="h-5 w-5 text-emerald-600" />

                  <p className="mt-4 text-2xl font-black text-slate-950">
                    92%
                  </p>

                  <p className="mt-1 text-xs font-semibold text-slate-500">
                    Security score
                  </p>
                </div>
              </div>

              <div className="grid gap-4 px-5 pb-5 lg:grid-cols-[1.1fr_0.9fr]">
                <div className="rounded-2xl border border-slate-200 bg-white p-5">
                  <div className="flex items-center justify-between">
                    <div>
                      <p className="text-sm font-extrabold text-slate-900">
                        Vault activity
                      </p>

                      <p className="mt-1 text-[11px] text-slate-500">
                        Last seven days
                      </p>
                    </div>

                    <span className="rounded-full bg-blue-50 px-2.5 py-1 text-[10px] font-bold text-blue-700">
                      +18%
                    </span>
                  </div>

                  <div className="mt-6 flex h-32 items-end gap-2">
                    {[35, 52, 42, 70, 58, 88, 76].map(
                      (
                        height,
                        index,
                      ) => (
                        <div
                          key={index}
                          className="flex flex-1 items-end"
                        >
                          <div
                            className="w-full rounded-t-lg bg-gradient-to-t from-blue-600 to-blue-400"
                            style={{
                              height: `${height}%`,
                            }}
                          />
                        </div>
                      ),
                    )}
                  </div>
                </div>

                <div className="rounded-2xl bg-slate-950 p-5 text-white">
                  <ShieldCheck className="h-6 w-6 text-blue-400" />

                  <p className="mt-5 text-lg font-extrabold">
                    Emergency plan ready
                  </p>

                  <p className="mt-2 text-xs leading-5 text-slate-400">
                    Two nominees are verified and your release preferences are configured.
                  </p>

                  <div className="mt-5 h-2 overflow-hidden rounded-full bg-white/10">
                    <div className="h-full w-[84%] rounded-full bg-blue-500" />
                  </div>

                  <p className="mt-2 text-[10px] font-bold uppercase tracking-wider text-slate-500">
                    84% complete
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div className="absolute -bottom-5 -left-4 hidden rounded-2xl border border-slate-200 bg-white p-4 shadow-xl sm:block">
            <div className="flex items-center gap-3">
              <span className="flex h-10 w-10 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600">
                <ShieldCheck className="h-5 w-5" />
              </span>

              <div>
                <p className="text-xs font-extrabold text-slate-900">
                  Security scan complete
                </p>

                <p className="mt-0.5 text-[10px] text-slate-500">
                  No suspicious activity found
                </p>
              </div>
            </div>
          </div>

          <div className="absolute -right-3 -top-5 hidden rounded-2xl border border-slate-200 bg-white p-4 shadow-xl sm:block">
            <p className="text-xs font-extrabold text-slate-900">
              Document protected
            </p>

            <p className="mt-1 text-[10px] text-slate-500">
              Insurance Policy.pdf
            </p>
          </div>
        </div>
      </div>
    </section>
  );
}import {
  BellRing,
  BrainCircuit,
  FileLock2,
  Fingerprint,
  ScanSearch,
  ShieldAlert,
  UsersRound,
  WandSparkles,
} from "lucide-react";
import { Link } from "react-router-dom";

import { ROUTES } from "../../utils/routePaths";

const features = [
  {
    icon: FileLock2,
    title: "Secure document vault",
    description:
      "Store financial, legal, identity and property documents in one protected digital workspace.",
    className:
      "bg-blue-50 text-blue-600",
  },
  {
    icon: UsersRound,
    title: "Trusted nominees",
    description:
      "Assign selected documents and permissions to people you trust without giving immediate access.",
    className:
      "bg-violet-50 text-violet-600",
  },
  {
    icon: ShieldAlert,
    title: "Emergency access",
    description:
      "Release approved information only after identity checks, evidence review and waiting periods.",
    className:
      "bg-amber-50 text-amber-600",
  },
  {
    icon: BrainCircuit,
    title: "AI organisation",
    description:
      "Automatically categorise documents, extract important information and generate useful summaries.",
    className:
      "bg-cyan-50 text-cyan-600",
  },
  {
    icon: ScanSearch,
    title: "Document intelligence",
    description:
      "Detect expiry dates, missing details, duplicates and potential document risks.",
    className:
      "bg-emerald-50 text-emerald-600",
  },
  {
    icon: BellRing,
    title: "Smart reminders",
    description:
      "Receive alerts about expiry dates, security events, nominee verification and account activity.",
    className:
      "bg-rose-50 text-rose-600",
  },
];

export default function FeaturesSection({
  showHeader = true,
}) {
  return (
    <section className="bg-white py-20 sm:py-24">
      <div className="mx-auto max-w-[1440px] px-4 sm:px-6 lg:px-8">
        {showHeader && (
          <div className="mx-auto max-w-3xl text-center">
            <div className="inline-flex items-center gap-2 rounded-full bg-violet-50 px-4 py-2 text-xs font-bold text-violet-700">
              <WandSparkles className="h-4 w-4" />
              Everything in one place
            </div>

            <h2 className="mt-5 text-3xl font-black tracking-tight text-slate-950 sm:text-4xl lg:text-5xl">
              A secure system for everything your family may need.
            </h2>

            <p className="mt-5 text-base leading-8 text-slate-600">
              LastKey combines document protection, access control,
              emergency planning and intelligent organisation in one
              professional platform.
            </p>
          </div>
        )}

        <div className="mt-12 grid gap-5 md:grid-cols-2 xl:grid-cols-3">
          {features.map(
            ({
              icon: Icon,
              title,
              description,
              className,
            }) => (
              <article
                key={title}
                className="group rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:border-blue-200 hover:shadow-xl hover:shadow-slate-200/60"
              >
                <span
                  className={`flex h-13 w-13 items-center justify-center rounded-2xl transition group-hover:scale-105 ${className}`}
                >
                  <Icon className="h-6 w-6" />
                </span>

                <h3 className="mt-6 text-xl font-extrabold text-slate-950">
                  {title}
                </h3>

                <p className="mt-3 text-sm leading-7 text-slate-500">
                  {description}
                </p>

                <Link
                  to={ROUTES.FEATURES}
                  className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-blue-600 transition group-hover:gap-3"
                >
                  Learn more
                  <span aria-hidden="true">→</span>
                </Link>
              </article>
            ),
          )}
        </div>

        <div className="mt-12 grid gap-5 rounded-[32px] bg-slate-950 p-6 text-white sm:p-8 lg:grid-cols-3">
          <div className="lg:col-span-1">
            <Fingerprint className="h-8 w-8 text-blue-400" />

            <h3 className="mt-5 text-2xl font-black">
              Security is not an add-on.
            </h3>

            <p className="mt-3 text-sm leading-7 text-slate-400">
              Protection is built into every LastKey workflow,
              from sign-in to document release.
            </p>
          </div>

          <div className="grid gap-4 sm:grid-cols-2 lg:col-span-2">
            {[
              "Role-based access controls",
              "JWT session protection",
              "Verified nominee workflow",
              "Complete activity history",
            ].map((item) => (
              <div
                key={item}
                className="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm font-bold text-slate-200"
              >
                {item}
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}import {
  CheckCircle2,
  Fingerprint,
  KeyRound,
  LockKeyhole,
  ScanLine,
  ShieldCheck,
} from "lucide-react";
import { Link } from "react-router-dom";

import Button from "../ui/Button";
import { ROUTES } from "../../utils/routePaths";

const securityPoints = [
  {
    icon: LockKeyhole,
    title: "Protected data",
    description:
      "Sensitive account and document information is designed to remain protected in storage and transit.",
  },
  {
    icon: Fingerprint,
    title: "Verified access",
    description:
      "Authentication, nominee verification and permission checks control every sensitive workflow.",
  },
  {
    icon: ScanLine,
    title: "Activity monitoring",
    description:
      "Important logins, document changes and emergency events can be recorded for audit review.",
  },
];

export default function SecuritySection() {
  return (
    <section className="overflow-hidden bg-slate-950 py-20 text-white sm:py-24">
      <div className="relative mx-auto grid max-w-[1440px] items-center gap-14 px-4 sm:px-6 lg:grid-cols-2 lg:px-8">
        <div className="absolute left-1/2 top-0 h-72 w-72 -translate-x-1/2 rounded-full bg-blue-500/10 blur-3xl" />

        <div className="relative">
          <div className="inline-flex items-center gap-2 rounded-full border border-blue-400/20 bg-blue-500/10 px-4 py-2 text-xs font-bold text-blue-300">
            <ShieldCheck className="h-4 w-4" />
            Security-first foundation
          </div>

          <h2 className="mt-6 max-w-xl text-3xl font-black tracking-tight sm:text-4xl lg:text-5xl">
            Your private information deserves serious protection.
          </h2>

          <p className="mt-5 max-w-xl text-base leading-8 text-slate-400">
            LastKey is designed around controlled access,
            identity verification, session security and clear
            audit history.
          </p>

          <div className="mt-8 space-y-5">
            {securityPoints.map(
              ({
                icon: Icon,
                title,
                description,
              }) => (
                <div
                  key={title}
                  className="flex gap-4"
                >
                  <span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl border border-white/10 bg-white/5 text-blue-300">
                    <Icon className="h-5 w-5" />
                  </span>

                  <div>
                    <h3 className="text-sm font-extrabold text-white">
                      {title}
                    </h3>

                    <p className="mt-1 text-sm leading-6 text-slate-400">
                      {description}
                    </p>
                  </div>
                </div>
              ),
            )}
          </div>

          <Link
            to={ROUTES.SECURITY}
            className="mt-8 inline-block"
          >
            <Button variant="secondary">
              Explore LastKey security
            </Button>
          </Link>
        </div>

        <div className="relative">
          <div className="absolute -inset-8 rounded-full bg-blue-600/10 blur-3xl" />

          <div className="relative rounded-[32px] border border-white/10 bg-white/5 p-5 backdrop-blur sm:p-7">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-300">
                  Security centre
                </p>

                <h3 className="mt-2 text-2xl font-black">
                  Account protection
                </h3>
              </div>

              <span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-blue-500 text-white shadow-lg shadow-blue-500/20">
                <KeyRound className="h-6 w-6" />
              </span>
            </div>

            <div className="mt-8 flex flex-col items-center gap-7 rounded-3xl border border-white/10 bg-slate-900/50 p-6 sm:flex-row">
              <div className="relative h-36 w-36 shrink-0">
                <svg
                  viewBox="0 0 100 100"
                  className="h-full w-full -rotate-90"
                >
                  <circle
                    cx="50"
                    cy="50"
                    r="43"
                    fill="none"
                    stroke="rgba(255,255,255,0.1)"
                    strokeWidth="8"
                  />

                  <circle
                    cx="50"
                    cy="50"
                    r="43"
                    fill="none"
                    stroke="#3b82f6"
                    strokeWidth="8"
                    strokeLinecap="round"
                    strokeDasharray="270"
                    strokeDashoffset="35"
                  />
                </svg>

                <div className="absolute inset-0 flex flex-col items-center justify-center">
                  <span className="text-3xl font-black">
                    87
                  </span>

                  <span className="text-[10px] font-bold uppercase tracking-wider text-slate-500">
                    Strong
                  </span>
                </div>
              </div>

              <div className="w-full space-y-3">
                {[
                  "Email verified",
                  "Strong password enabled",
                  "Access activity reviewed",
                  "Nominee verification pending",
                ].map(
                  (
                    item,
                    index,
                  ) => (
                    <div
                      key={item}
                      className="flex items-center justify-between rounded-xl border border-white/10 bg-white/5 px-4 py-3"
                    >
                      <span className="text-xs font-semibold text-slate-300">
                        {item}
                      </span>

                      <CheckCircle2
                        className={[
                          "h-4 w-4",
                          index < 3
                            ? "text-emerald-400"
                            : "text-amber-400",
                        ].join(" ")}
                      />
                    </div>
                  ),
                )}
              </div>
            </div>

            <div className="mt-5 grid gap-4 sm:grid-cols-2">
              <div className="rounded-2xl border border-white/10 bg-white/5 p-4">
                <p className="text-2xl font-black">
                  0
                </p>

                <p className="mt-1 text-xs text-slate-500">
                  Suspicious sessions
                </p>
              </div>

              <div className="rounded-2xl border border-white/10 bg-white/5 p-4">
                <p className="text-2xl font-black">
                  4
                </p>

                <p className="mt-1 text-xs text-slate-500">
                  Trusted devices
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}import {
  Check,
  Crown,
  ShieldCheck,
  Sparkles,
} from "lucide-react";
import { Link } from "react-router-dom";

import Button from "../ui/Button";
import { ROUTES } from "../../utils/routePaths";

const plans = [
  {
    name: "Essential",
    price: "₹0",
    period: "forever",
    description:
      "For individuals starting their secure digital vault.",
    icon: ShieldCheck,
    featured: false,
    features: [
      "Up to 10 documents",
      "One trusted nominee",
      "Basic expiry reminders",
      "Secure dashboard",
      "Account activity history",
    ],
  },
  {
    name: "Secure Plus",
    price: "₹299",
    period: "per month",
    description:
      "For complete family document and legacy management.",
    icon: Crown,
    featured: true,
    features: [
      "Up to 200 documents",
      "Five trusted nominees",
      "Emergency access workflow",
      "AI document organisation",
      "Advanced security centre",
      "Priority reminders",
    ],
  },
  {
    name: "Family",
    price: "₹599",
    period: "per month",
    description:
      "For families managing multiple secure digital vaults.",
    icon: Sparkles,
    featured: false,
    features: [
      "Up to five family profiles",
      "Shared family administration",
      "Advanced access permissions",
      "Expanded secure storage",
      "Priority support",
    ],
  },
];

export default function PricingSection({
  fullPage = false,
}) {
  return (
    <section
      className={
        fullPage
          ? "bg-slate-50 py-16 sm:py-20"
          : "bg-white py-20 sm:py-24"
      }
    >
      <div className="mx-auto max-w-[1440px] px-4 sm:px-6 lg:px-8">
        {!fullPage && (
          <div className="mx-auto max-w-3xl text-center">
            <p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-600">
              Flexible plans
            </p>

            <h2 className="mt-4 text-3xl font-black tracking-tight text-slate-950 sm:text-4xl lg:text-5xl">
              Start free. Upgrade when your vault grows.
            </h2>

            <p className="mt-5 text-base leading-8 text-slate-600">
              Choose the level of storage, nominee management
              and emergency access that fits your needs.
            </p>
          </div>
        )}

        <div
          className={[
            "grid gap-6 lg:grid-cols-3",
            fullPage ? "" : "mt-12",
          ].join(" ")}
        >
          {plans.map(
            (plan) => {
              const Icon =
                plan.icon;

              return (
                <article
                  key={plan.name}
                  className={[
                    "relative rounded-[30px] border p-6 sm:p-7",
                    plan.featured
                      ? "border-blue-600 bg-slate-950 text-white shadow-2xl shadow-blue-900/15"
                      : "border-slate-200 bg-white text-slate-950 shadow-sm",
                  ].join(" ")}
                >
                  {plan.featured && (
                    <span className="absolute right-5 top-5 rounded-full bg-blue-500 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-white">
                      Most popular
                    </span>
                  )}

                  <span
                    className={[
                      "flex h-12 w-12 items-center justify-center rounded-2xl",
                      plan.featured
                        ? "bg-blue-500/15 text-blue-300"
                        : "bg-blue-50 text-blue-600",
                    ].join(" ")}
                  >
                    <Icon className="h-6 w-6" />
                  </span>

                  <h3 className="mt-6 text-xl font-black">
                    {plan.name}
                  </h3>

                  <p
                    className={[
                      "mt-2 text-sm leading-6",
                      plan.featured
                        ? "text-slate-400"
                        : "text-slate-500",
                    ].join(" ")}
                  >
                    {plan.description}
                  </p>

                  <div className="mt-7 flex items-end gap-2">
                    <span className="text-4xl font-black">
                      {plan.price}
                    </span>

                    <span
                      className={[
                        "pb-1 text-xs font-semibold",
                        plan.featured
                          ? "text-slate-500"
                          : "text-slate-400",
                      ].join(" ")}
                    >
                      {plan.period}
                    </span>
                  </div>

                  <div
                    className={[
                      "my-7 h-px",
                      plan.featured
                        ? "bg-white/10"
                        : "bg-slate-200",
                    ].join(" ")}
                  />

                  <ul className="space-y-4">
                    {plan.features.map(
                      (feature) => (
                        <li
                          key={feature}
                          className="flex items-start gap-3"
                        >
                          <span
                            className={[
                              "mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full",
                              plan.featured
                                ? "bg-blue-500/15 text-blue-300"
                                : "bg-emerald-50 text-emerald-600",
                            ].join(" ")}
                          >
                            <Check className="h-3 w-3" />
                          </span>

                          <span
                            className={[
                              "text-sm",
                              plan.featured
                                ? "text-slate-300"
                                : "text-slate-600",
                            ].join(" ")}
                          >
                            {feature}
                          </span>
                        </li>
                      ),
                    )}
                  </ul>

                  <Link
                    to={ROUTES.REGISTER}
                    className="mt-8 block"
                  >
                    <Button
                      fullWidth
                      variant={
                        plan.featured
                          ? "primary"
                          : "secondary"
                      }
                    >
                      {plan.name ===
                      "Essential"
                        ? "Start free"
                        : "Choose plan"}
                    </Button>
                  </Link>
                </article>
              );
            },
          )}
        </div>

        <p className="mt-7 text-center text-xs text-slate-400">
          Pricing shown is for product-design demonstration and can be changed before launch.
        </p>
      </div>
    </section>
  );
}import CTASection from "../../components/landing/CTASection";
import EmergencySection from "../../components/landing/EmergencySection";
import FeaturesSection from "../../components/landing/FeaturesSection";
import HeroSection from "../../components/landing/HeroSection";
import HowItWorksSection from "../../components/landing/HowItWorksSection";
import PricingSection from "../../components/landing/PricingSection";
import SecuritySection from "../../components/landing/SecuritySection";
import TestimonialsSection from "../../components/landing/TestimonialsSection";

export default function LandingPage() {
  return (
    <div className="page-enter">
      <HeroSection />

      <FeaturesSection />

      <SecuritySection />

      <HowItWorksSection />

      <EmergencySection />

      <TestimonialsSection />

      <PricingSection />

      <CTASection />
    </div>
  );
}import {
  Outlet,
  useLocation,
} from "react-router-dom";
import { useEffect } from "react";

import LandingFooter from "../landing/LandingFooter";
import LandingNavbar from "../landing/LandingNavbar";

export default function LandingLayout() {
  const location = useLocation();

  useEffect(() => {
    window.scrollTo({
      top: 0,
      behavior: "instant",
    });
  }, [location.pathname]);

  return (
    <div className="min-h-screen bg-white">
      <LandingNavbar />

      <main>
        <Outlet />
      </main>

      <LandingFooter />
    </div>
  );
}